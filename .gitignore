# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# Move
build/
Move.lock
.trace
.coverage_map.mvcov

# Generated bindings — produced by `move-bindgen generate` against configs/.
/generated
/configs/*-rs

# Staging directories — produced by `move-bindgen fetch` next to each
# config (`<config-dir>/.move-bindgen-<config-stem>/`).
.move-bindgen

# Editors / OS
.DS_Store
.idea/
.vscode/
